#pragma once

#include <cstddef>

namespace zob
{
        const char* get_synth_bitstream_vera_sans_mono_data();
        std::size_t get_synth_bitstream_vera_sans_mono_size();
        const char* get_synth_bitstream_vera_sans_mono_name();
}

