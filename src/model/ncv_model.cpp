#include "ncv_model.h"

namespace ncv
{
//        //-------------------------------------------------------------------------------------------------

//        void model_t::test(const vector_t& input, const vector_t& target,
//                const loss_t& loss, stats_t& loss_stats, stats_t& error_stats)
//        {
//                const vector_t& output = process(input);

//                loss_stats.add(loss.value(target, output));
//                error_stats.add(loss.value(target, output));
//        }

//        //-------------------------------------------------------------------------------------------------
}
