language: cpp

sudo: false

compiler:
 - clang
 - gcc
 
addons:
  apt_packages:
    - time
    - cmake
    - ninja-build
    - libboost-dev 
    - libdevil-dev
    - libarchive-dev     
    
script:
 - "bash build_release.sh --build-sys make"
